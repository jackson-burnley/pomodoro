import React from "react";

const Timer = () => {
  return (
    <div className="Timer">
      <span className="Timer-span">25:00</span>

      <button>Start</button>
    </div>
  );
};

export default Timer;
